<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <title>Royal Contact App</title>

  <!-- Fonts & Icons -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600&display=swap" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- Global Theme (Royal SaaS) -->
  <style>
    :root {
      --royal-bg: linear-gradient(135deg, #2e0854, #3b0a4f, #11001c);
      --royal-gold: #f5c542;
      --royal-gold-soft: #ffe375;
    }

    body {
      background: var(--royal-bg);
      color: #fff;
      font-family: 'Poppins', sans-serif;
      min-height: 100vh;
    }

    /* 🔱 Navbar Styling */
    .navbar {
      background: rgba(46, 8, 84, 0.85);
      backdrop-filter: blur(12px);
      border-bottom: 1px solid rgba(255, 215, 0, 0.2);
      box-shadow: 0 0 20px rgba(255, 215, 0, 0.15);
      padding: 0.9rem 0;
      transition: all 0.3s ease-in-out;
    }

    .navbar:hover {
      box-shadow: 0 0 30px rgba(255, 215, 0, 0.25);
    }

    .navbar-brand {
      font-weight: 600;
      font-size: 1.5rem;
      color: var(--royal-gold) !important;
      letter-spacing: 1px;
      display: flex;
      align-items: center;
      gap: 8px;
      text-shadow: 0 0 12px rgba(255, 215, 0, 0.4);
    }

    .navbar-brand i {
      font-size: 1.4rem;
    }

    .nav-link {
      color: rgba(255, 255, 255, 0.85) !important;
      font-weight: 500;
      margin: 0 0.5rem;
      transition: all 0.3s ease;
      position: relative;
    }

    .nav-link::after {
      content: "";
      position: absolute;
      bottom: 0;
      left: 50%;
      transform: translateX(-50%) scaleX(0);
      transform-origin: center;
      height: 2px;
      width: 60%;
      background: var(--royal-gold);
      transition: transform 0.3s ease;
    }

    .nav-link:hover::after {
      transform: translateX(-50%) scaleX(1);
    }

    .nav-link:hover {
      color: var(--royal-gold) !important;
    }

    .navbar-toggler {
      border: none;
      color: var(--royal-gold);
      font-size: 1.2rem;
    }

    .navbar-toggler:focus {
      box-shadow: none;
    }

    /* For small devices */
    @media (max-width: 991px) {
      .nav-link {
        margin: 0.5rem 0;
      }
    }
  </style>
</head>

<body>
  <!-- 🔱 Navbar -->
  <nav class="navbar navbar-expand-lg fixed-top">
    <div class="container">
      <a class="navbar-brand" href="/">
        <i class="fa-solid fa-crown"></i> Royal Contacts
      </a>

      <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
        data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <i class="fa fa-bars"></i>
      </button>

      <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
        <ul class="navbar-nav text-center">
          <li class="nav-item">
            <a class="nav-link" href="/contacts">All Contacts</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/contacts/add">Add Contact</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <!-- Padding to prevent content overlap with fixed navbar -->
  <div style="padding-top: 80px;"></div>
